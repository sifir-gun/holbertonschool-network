#!/usr/bin/env bash
# Displays listening ports (TCP, UDP, and UNIX) with PID/Program name.

echo "Active Internet connections (only servers)"
netstat -lntup

echo "Active UNIX domain sockets (only servers)"
netstat -lxp
